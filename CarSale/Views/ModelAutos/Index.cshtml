@model List<ModelAuto>

@{
    ViewBag.Title = "Список моделей";
    Layout = "_Layout";
}

<div class="text-center">
    <h2>@ViewBag.Title</h2>
</div>

<div class="table_section">
    <a asp-action="Add" class="my-3 btn btn-outline-info">Добавить модель</a>

    @if (Model.Count == 0)
    {
        <p>Список брэндов пуст</p>
    }
    else
    {
        
        <table class="table mx-auto text-center">
            <thead>
            <tr>
                <th>№</th>
                <th>Название модели</th>
                <th>Редактирование</th>
                <th>Удаление</th>
            </tr>
            </thead>
            <tbody>
            @{
                int i = 0;
                foreach (var modelAuto in Model)
                {
                    <tr>
                        <td>@(i += 1)</td>
                        <td>@modelAuto.ModelName</td>
                        <td><a asp-action="Edit" asp-route-id="@modelAuto.Id" class="btn btn-primary">Редактироввть</a></td>
                        <td><a asp-action="Delete" asp-route-id="@modelAuto.Id" class="btn btn-danger">Удалить</a></td>
                    </tr>
                }
            }
            </tbody>
        </table>
    }
</div>